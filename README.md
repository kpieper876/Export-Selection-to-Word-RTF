# Export-Selection-to-Word-RTF
Export the current Obsidian Markdown selection—or the whole note when no text is selected—to a Word-readable Rich Text Format (RTF) file. The plugin converts common Markdown structure into portable document formatting without external tools.
